# Changelog

All notable changes to this repository are documented in this file.

## Unreleased

### Added

- `tooling/qr-generator` workspace package (`@seapay/qr-generator`) to build ERC-681 requests, SeaPay URLs, and generate QR codes.
- `packages/erc3009` workspace package (`@seapay/erc3009`) providing EIP-712 typed data helpers for TransferWithAuthorization (ERC-3009).
- `tooling/contracts` Foundry workspace for smart contracts.
- Documentation: `docs/DirectoryStructure.md`, `docs/ProjectGoals.md`, `docs/TASK.md`, `docs/CHANGELOG.md`.

### Changed

- pnpm workspace now includes `services/*` (in addition to `apps/*`, `packages/*`, `tooling/*`).


